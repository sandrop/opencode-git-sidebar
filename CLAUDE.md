# Project-specific instructions

## Task Management

This repo participates in the centralized cobtask system. The shared workflow rules live in the vault; this section only sets the per-repo config and imports them.

Tasks for this repo live in the central vault at `~/Documents/cobtasks/`, accessed via the `filesystem` MCP server (configured at user scope, available in every session).

**PROJECT**: `opencode-git-sidebar` # This repo's project tag
**REPO**:  `opencode-git-sidebar` # this repo's name
**PREFIX**:  `OCG-`  # Task ID prefix, three-letter task ID prefix (e.g., `GRN`)
**Project meta file:** `~/Documents/cobtasks/areas/opencode-git-sidebar.md`

@~/Documents/GitHub/sandrop/cobtask/cobtask-common-instructions.md

### Repo-specific overrides

(Add any rules that differ from or extend the shared instructions here. Most repos won't need this section.)
